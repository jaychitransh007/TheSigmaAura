# Test Suite

Canonical test-suite documentation is maintained at:
- `docs/context/test_suite.md`

Use this command to run all tests:
```bash
python3 -m unittest discover -s tests -p 'test_*.py' -v
```
